import React, {} from 'react'

const Spinner = ( props: Spinner.Props ) => (
  <span className="spinner-grow spinner-grow-sm" />
)

declare namespace Spinner {
  export interface Props {}
}

export default Spinner
